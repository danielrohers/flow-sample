{
  "name": "flow-sample",
  "description": "Flow JavaScript sample",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src/ -d lib/",
    "flow": "flow",
    "start": "npm run flow && npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.59.0"
  }
}
